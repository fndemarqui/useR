# Cleaning global environment and R search path

This function cleans up the global environment and unload all packages
loaded in a previous R session.

## Usage

``` r
start_clean_session()
```

## Value

No return value, called for side effects
